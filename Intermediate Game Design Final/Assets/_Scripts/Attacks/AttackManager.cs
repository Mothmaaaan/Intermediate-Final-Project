using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AttackManager : MonoBehaviour
{/*
    [Header("Current Attacks")]
    [SerializeField] List<Attack> currentAttacks = new List<Attack>();


#region Current Attack Array
// Add an attack to this array.
    public void AddAttack(Attack thisAttack){
        currentAttacks.Add(thisAttack);
    }

// Remove an attack from this array.
    public void RemoveAttack(Attack thisAttack){
        currentAttacks.Remove(thisAttack);
    }
#endregion

#region Pickup Attack
    private void OnTriggerEnter(Collider other) {
        if(other.CompareTag("Pickup")){
            // Determine what attack this is.
        }
    }

#endregion*/
}
